{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
"env": {
  "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}",
  "APP_PASSWORD": "${APP_PASSWORD}"
}
}
